const calculate(num, secondnum, operation){
  if(operation==='+'){
    return num + secondnum;
  }
  if(operation==='-'){
    return num - secondnum;
  }
  if(operation==='/'){
    return num / secondnum;
  }
  if(operation==='*'){
    return num * secondnum;
  }
}
keys.addEventListener('click'{
  if (this.target.matches('click')) {


    if ('number') {
    }

    if (
      'add' || 'subtract' || 'multiply' ||'divide'){
    }

    if ('point') {
    }
    
    if (action === 'clear') {
      display = 0;
    }
    if (action === 'calculate') {
      const num1
      const num2
      const operator
      calculate(num1,num2,operator)
    }
  }